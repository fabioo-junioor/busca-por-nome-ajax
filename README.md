#### Demonstra a comunicação da página com o servidor WEB enquanto o usuário digita caracteres em um campo.  
- Cada vez que o usuário digita um caractere no campo input a função javascript showHint() é executada.